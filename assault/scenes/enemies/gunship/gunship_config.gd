## GunshipConfig — configuration for Gunship.
## Gunship manages its own entry/hold/retreat movement via AI — no movement_speed here.
class_name GunshipConfig
extends ShipConfig

@export var fire_interval: float = 0.6
